import { HighlightExpDirective } from './highlight-exp.directive';

describe('HighlightExpDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightExpDirective();
    expect(directive).toBeTruthy();
  });
});
